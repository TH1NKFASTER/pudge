Metadata-Version: 2.4
Name: anime-mpv
Version: 0.6.70
Summary: macOS anime library manager with AniList, Nyaa, qBittorrent, mpv and Japanese subtitles
Author: anime-mpv
Requires-Python: >=3.11
Description-Content-Type: text/markdown
Requires-Dist: httpx<1,>=0.27
Requires-Dist: rapidfuzz<4,>=3.9
Requires-Dist: charset-normalizer<4,>=3.3
Requires-Dist: pillow<13,>=10
Requires-Dist: socksio<2,>=1.0
Requires-Dist: pywebview<7,>=5.4
Requires-Dist: pyobjc-core>=10.3; sys_platform == "darwin"
Requires-Dist: pyobjc-framework-Cocoa>=10.3; sys_platform == "darwin"
Requires-Dist: pyobjc-framework-Quartz>=10.3; sys_platform == "darwin"
Requires-Dist: pyobjc-framework-Vision>=10.3; sys_platform == "darwin"
Requires-Dist: pyobjc-framework-WebKit>=10.3; sys_platform == "darwin"
Requires-Dist: pyobjc-framework-Security>=10.3; sys_platform == "darwin"
Requires-Dist: pyobjc-framework-UserNotifications>=10.3; sys_platform == "darwin"
Provides-Extra: sync
Requires-Dist: ffsubsync<0.5,>=0.4.31; extra == "sync"
Provides-Extra: dev
Requires-Dist: pytest<9,>=8; extra == "dev"
Requires-Dist: ruff<1,>=0.9; extra == "dev"

# Pudge

Pudge is a macOS anime library/player manager built around AniList, Nyaa, qBittorrent/aria2, mpv and Japanese subtitles.

Current version: **0.6.70**.

## Install a release

Download `pudge-macos-vX.Y.Z.zip` from GitHub Releases, then:

```bash
cd ~/Downloads
rm -rf pudge
unzip pudge-macos-v0.6.70.zip
cd pudge
./install.sh
```

The installer sets up the managed Python environment and required macOS/Homebrew tools.

## Development

```bash
python3.12 -m venv .venv
source .venv/bin/activate
python -m pip install --upgrade pip
python -m pip install -e ".[dev,sync]"
```

Run the whole test suite normally:

```bash
make test
```

Or run the whole suite in deterministic batches:

```bash
make test-batches
```

See [DEVELOPMENT.md](DEVELOPMENT.md) for details.

## Releases

Version tags are automated. Pushing a tag such as `v0.6.69` runs the full test suite in four macOS batches, builds the release ZIP and attaches it to a GitHub Release.

See [RELEASING.md](RELEASING.md).

## Release notes

Historical release notes are kept in [CHANGELOG.md](CHANGELOG.md).
